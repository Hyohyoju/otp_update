# 🔰 처음 사용자를 위한 3분 완성 가이드

이 문서는 **아두이노/ESP32 초보자**가 이 템플릿을 사용하여 **무선 업데이트(OTA)** 기능을 장치에 넣는 과정을 설명합니다.

---

## 1단계: 내 정보 입력하기 📝
가장 먼저 `UserConfig.h` 파일을 열어서 빈칸을 채워주세요.

1. **와이파이 이름/비번**: ESP32가 연결할 와이파이 정보를 적습니다.
2. **다운로드 주소**: 펌웨어 파일이 저장될 인터넷 주소를 적습니다. (아직 파일이 없어도 주소부터 정해두면 좋습니다)

```c
// 예시
const char *ssid = "우리집_와이파이";
const char *password = "비밀번호_1234";
const char *firmware_url = "https://내_홈페이지.com/update.bin"; 
```

---

## 2단계: 최신 업데이트 파일 만들기 📦
장치를 업데이트하고 싶을 때마다 이 단계를 수행합니다.

1. 코드를 수정합니다 (예: LED 깜빡임 속도 변경 등).
2. `UserConfig.h`에서 버전을 `1`에서 `2`로 바꿔줍니다. (업데이트 확인용)
   ```c
   #define CURRENT_FIRMWARE_VERSION 2
   ```
3. VS Code나 아두이노 IDE에서 **[컴파일]** 버튼을 누릅니다. (업로드 X)
4. 생성된 `.bin` 파일을 찾습니다. (보통 `build` 폴더 안에 있습니다)
5. 파일 이름을 `update.bin` (설정한 이름)으로 바꿉니다.

---

## 3단계: 인터넷에 파일 올리기 ☁️
방금 만든 `.bin` 파일을 1단계에서 적어둔 주소(`firmware_url`)에 올립니다.

- **GitHub 사용 시**: 파일을 저장소에 올리고 `Commit` & `Push` 하면 끝!
- **개인 서버 사용 시**: FTP 등을 이용해 파일을 덮어쓰기 합니다.

---

## 4단계: 장치 켜기 (자동 업데이트) 🚀
이제 장치(ESP32)의 전원을 켜기만 하면 됩니다.

1. 장치가 켜지면 자동으로 와이파이에 연결합니다.
2. 인터넷 주소에 가서 새 파일이 있는지 확인합니다.
3. **새 파일이 있다면 스스로 다운로드하고 재부팅합니다.**
4. "버전 2"로 짠! 하고 바뀝니다. ✨

---

## ⚡ 배포하기 (자동화 스크립트) ⚡

매번 버전 바꾸고, 파일 이름 바꾸고... 귀찮으신가요?
제가 만들어드린 `deploy.py`를 실행하면 이 모든 걸 자동으로 해줍니다!

### 사용 방법
1. 터미널(Terminal)을 엽니다.
2. 아래 명령어를 입력합니다.
   ```bash
   python deploy.py
   ```
3. 스크립트가 시키는 대로만 하면 끝!
   - 자동으로 버전(`CURRENT_FIRMWARE_VERSION`)을 1 올려줍니다.
   - **"컴파일 하세요"** 라고 뜨면 컴파일 버튼만 누르고 엔터를 칩니다.
   - 알아서 파일을 찾아서 이름도 바꾸고, **GitHub에 올리기까지 다 해줍니다.**

이제 커피 한 모금 마시는 사이에 배포가 끝납니다. ☕

---

## 🏗️ 8개 기기(여러 대)를 각각 따로 관리하는 법
기기마다 동작이 다르거나 업데이트 내용이 다르다면, **"각 기기마다 전용 주소(사서함)"**를 만들어주면 됩니다.

### 예시: 기기 1번과 2번이 서로 다른 코드를 써야 할 때

#### 1. GitHub에 파일 이름을 다르게 올리기
GitHub 저장소에 펌웨어 파일을 각각 다른 이름으로 올립니다.
- 기기 1번용 파일: `device1_update.bin`
- 기기 2번용 파일: `device2_update.bin`

#### 2. 각 기기의 UserConfig.h 설정하기
코드를 업로드할 때, `firmware_url`을 서로 다르게 설정해줍니다.

**[기기 1번 코드]**
```cpp
const char *firmware_url = "https://.../device1_update.bin";
```
**[기기 2번 코드]**
```cpp
const char *firmware_url = "https://.../device2_update.bin";
```

### 3. 업데이트 진행 방법
- **기기 1번을 바꾸고 싶을 때**:
  1. 기기 1번용 코드를 수정하고 컴파일합니다.
  2. 나온 파일 이름을 `device1_update.bin`으로 바꿉니다.
  3. GitHub에 올립니다.
  4. -> **기기 1번만** 이 파일을 다운로드하여 업데이트합니다. (2번은 영향 없음)

- **기기 2번을 바꾸고 싶을 때**:
  1. 기기 2번용 코드를 수정하고 컴파일합니다.
  2. 나온 파일 이름을 `device2_update.bin`으로 바꿉니다.
  3. GitHub에 올립니다.
  4. -> **기기 2번만** 이 파일을 다운로드하여 업데이트합니다.
